---
description: Core workflow harness — read config and docs every session
alwaysApply: true
---

# Workflow core

1. Read `workflow.config.md` at repo root.
2. Read `docs/workflow/next_actions.md` when `JIRA_TASKS=OFF`.
3. Follow `docs/workflow/test-lane.md` when closing code deliveries.

Implementer does not run formal validation lane as self-gate. Use isolated runners per `VALIDATION_CURSOR` in config.

Methodology: SDD adaptive + merge gates in `docs/workflow/gates.md`.
