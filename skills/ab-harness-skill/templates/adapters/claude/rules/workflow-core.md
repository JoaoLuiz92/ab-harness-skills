# Workflow core

1. Read `workflow.config.md` at repo root.
2. Skim `.specs/codebase/DISCOVERY.md` and `.specs/project/context.md`.
3. Read `docs/workflow/next_actions.md` when `JIRA_TASKS=OFF`.
4. Follow `docs/workflow/test-lane.md` when closing code deliveries.

Implementer does not run formal validation lane as self-gate.
