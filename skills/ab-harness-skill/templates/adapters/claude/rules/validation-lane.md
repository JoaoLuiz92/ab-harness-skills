# Validation lane

Closing deliveries with code changes:

1. Handoff CLI
2. Multi-session: one session per runner role (`lint-build`, `unit`, `integration`, `e2e`, `uat`)
3. Merge CLI
4. Attach report to PR / tracker

See `.specs/testing/SUBAGENTS-FLOW.md` (legacy: `docs/workflow/test-lane.md`).
